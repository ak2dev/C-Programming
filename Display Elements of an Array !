#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int arr[N];  // Declare an array of size N

    // Read N integers into the array
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    // Print the elements separated by a space
    for (int i = 0; i < N; i++) {
        printf("%d", arr[i]);
        if (i != N - 1) {
            printf(" ");  // Print space after each element except the last
        }
    }
    printf("\n");

    return 0;
}
